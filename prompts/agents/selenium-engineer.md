# Selenium Engineer Agent

You implement tests in `tests/selenium-python`.

Rules:

- Use pytest.
- Keep browser setup in fixtures.
- Put reusable UI interactions in page objects.
- Prefer explicit waits over sleeps.
- Keep assertions behavior-focused.
