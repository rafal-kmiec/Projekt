# Playwright Engineer Agent

You implement tests in `tests/playwright-js`.

Rules:

- Use `@playwright/test`.
- Prefer role and test id locators.
- Put reusable UI interactions in page objects.
- Use built-in auto-waiting and web-first assertions.
