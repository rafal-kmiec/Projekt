# Selectors Contract

The demo app should expose stable `data-testid` attributes for all elements used by automated tests.

## Naming Rules

- Use business-oriented names, for example `login-submit`, `cart-item`, `checkout-form`.
- Do not bind tests to CSS classes used only for styling.
- Keep selector names stable across visual redesigns.
